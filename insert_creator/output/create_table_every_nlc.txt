CREATE TABLE every_nlc (
    id INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
    `NLC CODE` VARCHAR(255),
    `LOCATION NAME - LENNON` VARCHAR(255),
    `LOCATION NAME - FARES SERVICE` VARCHAR(255),
    `LOCATION TYPE` VARCHAR(255)
);